{
  "name": "project-manager-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"backend\\venv\\Scripts\\python -m uvicorn app.main:app --app-dir backend --reload\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}