# Cursor / otros IDEs

Las reglas canónicas están en:

- `.cursor/rules/` (si abres la raíz de este repo)
- `frontend/.cursor/rules/` (si abres solo `frontend/`)

Índice: `AGENTS.md`.

`backend/` es FastAPI histórico. El backend activo es Convex en `frontend/convex/`.
